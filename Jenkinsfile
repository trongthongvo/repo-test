pipeline {

    //agent none
    agent {label 'slave'}

    stages {

        stage('Build') {
            steps {
            
                sh '''
                    ./shell.sh
                '''
            }

            
                }
            }
 
}
